name: Test skills

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  minimalist-html-ppt:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v5
        with:
          node-version: 22
      - name: Validate the bundled template
        run: node skills/minimalist-html-ppt/scripts/check-editorial-deck.js skills/minimalist-html-ppt/assets/editorial-web-deck/template.html
  video-subtitle-translator:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v5
        with:
          node-version: 22
      - name: Check script syntax
        run: node --check skills/video-subtitle-translator/scripts/subtitle-tools.mjs
      - name: Run tests
        run: node --test skills/video-subtitle-translator/tests/*.test.mjs
